[ { "BlackBox" :
    { "name"      : "CLaSH.Sized.VectorZ.vmap",
      "template"  :
"for n in (~RESULT'length - 1) downto 0 generate
begin
  ~INST 0
    ~OUTPUT => ~RESULT(n)~
    ~INPUT  => ~ARG[1](n)~
  ~INST
end generate;",
      "templateI" : ""
    }
  }
]
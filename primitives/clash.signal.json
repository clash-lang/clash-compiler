[ { "BlackBox" :
    { "name"      : "CLaSH.Signal.register",
      "inputs"    : 2,
      "litInputs" : 0,
      "template"  : "process(defaultClockReset,defaultClock) is
  begin
    if defaultClockReset = '0' then
      {{result.val}} <= {{inputs.0.val}};
    elsif rising_edge(defaultClock) then
      {{result.val}} <= {{inputs.1.val}};
    end if;
end process;"
    }
  }
, { "BlackBox" :
    { "name"      : "CLaSH.Signal.sync",
      "inputs"    : 1,
      "litInputs" : 0,
      "template"  : "{{result.val}} <= {{varInputs.0.val}};"
    }
  }
, { "BlackBox" :
    { "name"      : "CLaSH.Signal.mapSync",
      "inputs"    : 2,
      "litInputs" : 0,
      "template"  : "MAPSYNC"
    }
  }
, { "BlackBox" :
    { "name"      : "CLaSH.Signal.appSync",
      "inputs"    : 2,
      "litInputs" : 0,
      "template"  : "APPSYNC"
    }
  }
]
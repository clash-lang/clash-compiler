---
issues: [2978]
prs: [3140]
---

# FIXED
`deepErrorX` and `ensureSpine` on a `Bit` no longer throws an `XException`; it now produces an undefined `Bit` (`.`), matching the behavior of `deepErrorX` on `BitVector n`.

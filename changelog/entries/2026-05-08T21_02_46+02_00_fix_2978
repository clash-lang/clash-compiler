FIXED: `deepErrorX` and `ensureSpine` on a `Bit` no longer throws an `XException`; it now produces an undefined `Bit` (`.`), matching the behavior of `deepErrorX` on `BitVector n`. See [#2978](https://github.com/clash-lang/clash-compiler/issues/2978).
